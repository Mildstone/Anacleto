{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "ghdl",
            "type": "shell",
            "command": "make ghdl-clean ghdl-core ghdl-run NAME=axis_8b10b UNIT=io_tb",
            "options": {
                "cwd": "${env:abs_builddir}"
            },
            "problemMatcher": [
                "$eslint-compact"
            ]
        }
    ]
}